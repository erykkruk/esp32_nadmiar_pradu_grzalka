[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gcc", "gnumake"]
cmds = ["corepack enable && corepack prepare pnpm@9.15.4 --activate"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "pnpm start"
