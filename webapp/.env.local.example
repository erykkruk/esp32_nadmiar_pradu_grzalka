# Secret key for ESP32 authentication (must match ESP32 sketch)
API_KEY=change-me-to-a-random-secret

# SQLite database path (relative to project root)
DB_PATH=./data/readings.db

# Max database size in MB before pruning oldest records
DB_MAX_SIZE_MB=200
